# SoundGrid — מפת דרכים / Roadmap

תוכנית פיתוח מ‑MVP ועד `v1.0`.
כל גרסה עומדת בפני עצמה (בונה, נדחפת, שמישה). סדר הפיצ'רים גמיש — אפשר להזיז לפי צורך.

> כיווני ארכיטקטורה ארוכי‑טווח (תפרים/ports, שכבת AI, בידול מול Serato/rekordbox,
> חומרה): [`docs/architecture/directions.md`](docs/architecture/directions.md).
> גרסאות ה‑`.5` למטה נגזרות משם.
>
> **הקובץ הזה מחזיק את מה שעוד לא נבנה.** גרסה שנסגרת יוצאת מכאן ל‑
> [`docs/handoff/`](docs/handoff/), לקובץ אחד שמחזיק גם את התכולה וגם את התוצאה.

**עקרונות מנחים**
- Chromium‑first (Web Audio / Web MIDI / File System Access / `setSinkId`).
- Controller‑first: כל פקד שעובד בעכבר חייב לעבוד גם דרך MIDI.
- אפס קוד/נכסים קנייניים מ‑Serato / rekordbox. קריאת תגיות מקבצים של המשתמש בלבד.
- כל שינוי מלווה ב‑build ירוק (`npm run build`) ובדיקת lint.

**סכימת גרסאות:** `0.MINOR.PATCH` עד `v1.0.0`. MINOR זוגי‑עגול = אבן דרך גדולה;
`.5` (v0.1.5, v0.4.5…) = יחידה קטנה/גזירה מ‑`directions.md` שנכנסת בין אבני הדרך.

| גרסה | נושא | סטטוס |
| --- | --- | --- |
| v0.1.0 | MVP — דקים, מיקסר, ספרייה, waveform, MIDI, פריסט FLX4 | ✅ הושלם |
| v0.1.5 | **Design overhaul** — שפת עיצוב חדשה, יפה מסראטו/רקורדבוקס | ✅ אושר ויזואלית ע"י המשתמש (2026-08-28) |
| v0.1.6 | **Seams** — ports/interfaces לפני שהקוד גדל (audio, transport, clock, caps) | ✅ |
| v0.1.7 | **Tag read** — קריאת BPM/key/duration/artist מתגיות הקבצים בזמן סריקה | ✅ |
| v0.2.0 | Jog wheels & scratching | ✅ (2026-09-02) — נסגרה על פני v0.2.2·v0.2.3·v0.2.4; `JOG_TICKS_PER_REV=713` נמדד מול החומרה |
| v0.2.1 | **אינווריאנטים ברמת הריפו** — `vitest` נכנס, `tests/repo/` אוכף את כללי המסמכים | ✅ (2026-08-29) |
| v0.2.2 | **סגירת 3 מ‑5 הפתוחים של v0.2.0** — ציר הגרירה, הסף בפלייהד, bend במקלדת | ✅ (2026-08-29) |
| v0.2.3 | **הפלטר בגודל שאפשר לגרד עליו** — 54→142px, נמדד בדפדפן | ✅ (2026-08-29) |
| v0.2.4 | **‏`CLAUDE.md` הכריז את עצמו ואת ההיפך ממנו** — סימני קונפליקט מקומיטים, והבדיקה שתופסת אותם | ✅ (2026-08-30) |
| v0.2.5 | **מסך Settings** — כיול הבקר, תחושת הפקדים והעדפות תצוגה, ומקום אחד לשמירה שלהם | ✅ (2026-08-30) |
| v0.2.6 | **פתיחה עם הספרייה כבר טעונה** — זוכר את התיקייה האחרונה, ומציע את Music למשתמש חדש | ✅ (2026-09-02) |
| v0.2.7 | **‏`strict` מופעל** — הדגל ש‑`CLAUDE.md` הכריז עליו שבע גרסאות בלי שאף `tsconfig` הפעיל אותו | ✅ (2026-09-02) — **אפס שגיאות** |
| v0.2.8 | **‏יבוא מהבראנץ׳ המקביל** — ‏`N unreadable` בספרייה, ורשומה שמורה שלא ניתנת להחייאה | ✅ (2026-09-02) |
| v0.2.9 | **‏`context_check.py` נכנס לריפו** — הכלי שהנוהל דורש ולא היה קיים בשיחה מרוחקת | ✅ (2026-09-02) |
| v0.3.0 | **Beatgrid & phase‑sync** — רשת ביט אמיתית, SYNC שנועל פאזה, quantize | ✅ (2026-09-02) |
| v0.3.1 | מספר גרסה בקליינט — מוצג ומסונכרן תמיד ל‑`package.json` | ✅ (2026-09-03) |
| v0.3.2 | **עמודת ז'אנר** — נגזרת מתיקיית האב בסריקה, עריכה ידנית מרשימה סגורה | ✅ (2026-09-02) |
| v0.4.0 | ניתוח מתמשך + מטא‑דאטה קבועה | ✅ (2026-09-03) |
| v0.4.5 | סימון "השיר הבא" — הדגשת טראקים תואמים ל‑BPM/סולם של הדק המנגן | ✅ (2026-09-04) |
| v0.4.7 | קרבת אנרגיה (מיקס) — נדחה מ‑v0.4.5, אין איפיון עדיין | ⬜ |
| v0.4.8 | **Hint mode** — "?" ליד כל פקד באפליקציה, כולל מקבילה ב‑FLX4 | ✅ (2026-09-04) |
| v0.5.0 | Pad modes | ⬜ |
| v0.5.5 | שליטה בשפה טבעית / קול → `ControlAction` | ⬜ |
| v0.6.0 | Sampler / performance decks | ⬜ |
| v0.7.0 | FX units | ⬜ |
| v0.8.0 | ניהול ספרייה — crates, עמודות, היסטוריה | ⬜ |
| v0.8.5 | חיפוש סמנטי בספרייה (embeddings על האודיו) | ⬜ |
| v0.9.0 | זיהוי סולם + מיקס הרמוני | ⬜ |
| v0.9.5 | מצב קואצ'ינג — ביקורת AI על הסט (drift, EQ, מעברים, gain) | ⬜ |
| v0.10.0 | הקלטה + שידור | ⬜ |
| v0.11.0 | עורך מיפוי חומרה + LED feedback | ⬜ |
| v0.11.5 | Plugin API — `controls.ts` ציבורי, pad modes/FX/סקריפטים קהילתיים | ⬜ |
| v0.12.0 | שדרוג waveform (overview + צבע תדרים) | ⬜ |
| v0.13.0 | Auto‑mix / מעברים חכמים | ⬜ |
| v0.13.5 | קו‑פיילוט מיקס חי — הצעת טראק הבא + אזהרות בזמן אמת | ⬜ |
| v0.14.0 | מצב 4 דקים | ⬜ |
| v0.15.0 | מקורות ענן / סטרימינג + locker | ⬜ |
| v0.16.0 | Cue/loop שמורים לכל טראק + ייבוא תגיות | ⬜ |
| v0.17.0 | ליטוש UI + layouts + מגע | ⬜ |
| v0.18.0 | Latency ואיכות אודיו (AudioWorklet) | ⬜ |
| v0.19.0 | MIDI clock / Ableton Link | ⬜ |
| v0.20.0 | Stems + הפרדה בזמן אמת | ⬜ |
| v1.0.0 | יציבות, PWA, onboarding, טסטים, אריזה | ⬜ |

---

## גרסאות סגורות

התכולה המלאה ומה שנעשה בפועל עברו ל‑[`docs/handoff/`](docs/handoff/) — קובץ אחד לגרסה,
שמחזיק גם את האיפיון המקורי וגם את התוצאה והמדידות. **הקבצים האלה לא נקראים בתחילת
שיחה.** הקובץ הזה מחזיק מכאן והלאה **רק את מה שעוד לא נבנה.**

| גרסה | הרשומה | השורה התחתונה |
| --- | --- | --- |
| v0.1.5 — Design Overhaul | [`docs/handoff/v0.1.5.md`](docs/handoff/v0.1.5.md) | אושר ויזואלית מול Serato על אותו מסך. **⚠️ סבב `ui-ux-review` + `drop-generic-design` "אחרי" לא בוצע** |
| v0.1.6 — Seams | [`docs/handoff/v0.1.6.md`](docs/handoff/v0.1.6.md) | ל‑`core/` שלושה imports, כולם פנימיים. `npm run check` = tsc + oxlint + depcruise |
| v0.1.7 — Tag read | [`docs/handoff/v0.1.7.md`](docs/handoff/v0.1.7.md) | BPM 96.9% · Key 97.2% · Duration 100% · 0.6ש' על 360 קבצים |

---

## v0.2.0 — Jog Wheels & Scratching 🔶 בעבודה

**מטרה:** לגעת בג'וג של ה‑FLX4 ולנגן איתו — scratch, pitch‑bend, עצירה.

- **v0.2.0a — מנוע ✅.** `AudioBufferSourceNode` של Chrome לא מסוגל לגרד (במהירות
  ‎-1/‎-0.5/0 מצביע הקריאה קופא ופולט DC). הוחלף ב‑`AudioWorklet` עם מצביע חתום,
  מאחורי `SourcePlayer` עם נפילה חזרה **גלויה**. ניגון רגיל זהה ביט‑לביט.
- **v0.2.0b — המחווה 🔶.** הפלטר הגריר, מצב ויניל, עצירת הוויניל ופיצול הג'וג ב‑MIDI
  נכתבו. **ארבעה סעיפים מהתכנון מעולם לא בוצעו** — גודל הפלטר, ציר הגרירה, סף
  ה‑`useRenderLoop`, ו‑bend במקלדת.
- **הושלם כאשר:** אפשר לגרד ידנית עם הג'וג ובעכבר, ולנגן bend, בלי ש‑playhead קופץ.

**הפתוח מפורט ב‑[`HANDOFF.md`](HANDOFF.md).** הרשומה המלאה — המדידות, שלושת מסלולי
הכשל של ה‑worklet, והמלכודות — ב‑[`docs/handoff/v0.2.0.md`](docs/handoff/v0.2.0.md).

## v0.2.1 — אינווריאנטים ברמת הריפו ✅
**מטרה:** שהכללים שמנהלים את המסמכים ייאכפו, אחרי שהפיצול (v0.2.0, 29/08) כתב אותם
אבל השאיר אותם מוסכמות. הפיצול הציב תקציב בראש `HANDOFF.md` ואף אחד לא בדק אותו.
- **`vitest`** — מסגרת הבדיקות הראשונה בריפו. `npm test`, ובתוך `npm run check`.
  ‏`ROADMAP` תיזמן טסטים ל‑v1.0.0; זה היה מאוחר מדי, כי בלי runner האינווריאנטים
  לא ניתנים לביטוי בכלל.
- **חמישה אינווריאנטים ב‑`tests/repo/`** — תקציב הגודל, גרסה מסונכרנת בין
  `package.json` ל‑`HANDOFF.md`, נתיבים חיים במסמכים (בשתי הצורות שהם כותבים),
  SHA שמתרזולב, ושני קבצי ה‑CLAUDE שזזים באותו commit. **כל אחד באג שקרה בפועל כאן.**
- **לא בתכולה:** בדיקות להתנהגות האפליקציה. מנוע האודיו דורש `AudioContext`
  והספרייה דורשת קבצים אמיתיים; זיוף היה בודק את הזיוף. הלוגיקה הטהורה ב‑`core/`
  היא המקום הבא.
- **הושלם כאשר:** כל בדיקה הוכחה נופלת בלי התיקון שלה, ו‑`npm run check` ירוק.
  פירוט: [`docs/handoff/v0.2.1.md`](docs/handoff/v0.2.1.md).

## v0.2.2 — סגירת שלושה מהפתוחים של v0.2.0 ✅
**מטרה:** לסגור את הסעיפים מתכנון v0.2.0b שניתן לסגור **בלי חומרה ובלי מסך** —
ולתת להם בדיקות, כי היעדרן הוא הסיבה שהם שרדו.
- **`core/scratch.ts`** — הלוגיקה יצאה מהקומפוננטות ל‑`core/`, ואיתה הבדיקות
  הראשונות להתנהגות האפליקציה בריפו (12 מקרים).
- **גרירה לפי ציר X במקום זווית** — הגיין כבר לא תלוי באיפה תפסת (נמדד פי 5.0
  על הפלטר הנוכחי).
- **הסף בפלייהד** — יד על הפלטר עוקפת אותו; בלי זה גירוד איטי הקפיא את המצביע.
- **bend במקלדת** — `S`/`D` ו‑`K`/`L`, ±4% בהחזקה, שחרור גם ב‑`blur`.
- **לא בתכולה:** סעיף 1 (גודל הפלטר) — שינוי פריסה שהקריטריון שלו הוא מדידה על
  המסך של המשתמש; וסעיף 5 — בדיקה מול חומרה.

## v0.2.3 — הפלטר בגודל שאפשר לגרד עליו ✅
**מטרה:** הסעיף האחרון של v0.2.0 שלא דורש חומרה. הפלטר היה 54px — ‏**10.9 מ"מ**
על המסך של המשתמש, פקד שמכוונים אליו ולא מגרדים בו.
- **142px** (‏28.6 מ"מ), רצפה מוצהרת: לא לשלוח פלטר אינטראקטיבי מתחת ל‑120 CSS px.
- **הפלטר והטמפו זה לצד זה**, לא מוערמים — הערימה היא מה שכפה 54px מלכתחילה.
  צ'יפ ה‑VINYL עבר מתחת לפלטר, שם הוא מתאר את המצב של הפקד שמעליו.
- **נמדד בדפדפן** (Chromium בקונטיינר, 1536×710) לפני ואחרי: גובה השורה נשאר
  **178px**, גובה הדק **364**, הספרייה **269**, ואין גלילה אנכית או אופקית.
  פירוט: [`docs/handoff/v0.2.3.md`](docs/handoff/v0.2.3.md).

## v0.2.4 — הקובץ הקובע סתר את עצמו ✅
**מטרה:** ‏`CLAUDE.md` — הקובץ שנטען אוטומטית לכל שיחה — ישב על `main` עם **שני
אזורי מיזוג לא‑פתורים** מקומיטים בתוכו, מ‑v0.2.1 ועד v0.2.3. שני הצדדים נשמרו
כלשונם, אז אותו סעיף אמר גם "האינווריאנטים נאכפים ב‑`tests/repo/`" וגם "שום דבר
לא אוכף את זה, ו‑`package.json` עדיין `0.0.0`" — צד שהיה שקרי מאז v0.2.1.
- **הפתרון נקבע לפי `CLAUDE-HE.md`**, שכבר החזיק את הגרסה הממוזגת הנכונה. שני
  הקבצים שוב אומרים את אותו דבר, ושניהם קיבלו את הפסקה על `ROADMAP.md`.
- **`npm run check` היה ירוק כל הזמן הזה** — וזה הממצא, לא התקלדה: חמש בדיקות
  הריפו קוראות את המסמכים **למשמעות** (גדלים, נתיבים, SHA‑ים, גרסה, עריכה
  משותפת), ואף אחת לא קראה אותם **לנזק**.
- **בדיקה שישית:** `tests/repo/no-conflict-markers.test.ts` — כל קובץ טקסט
  במעקב, לא רק מסמכים. הופל על ארבע השורות לפני התיקון.
  ‏`version-in-step` הורחב גם לנעילה, שעדיין אמרה `0.0.0` מ‑v0.2.1.
  פירוט: [`docs/handoff/v0.2.4.md`](docs/handoff/v0.2.4.md).

## v0.2.5 — מסך Settings ✅

> **למה זה עקף את "פתיחה עם הספרייה כבר טעונה" (30/08).** הסבב מול ה‑FLX4 הראה
> ששינוי של מספר אחד עולה מחזור קוד→push→pull מלא, ושלוש פעמים הלוך‑ושוב על
> `BEND_PER_TICK` לבדו. ‏`JOG_TICKS_PER_REV` עדיין לא נמדד ו**לא ניתן למדידה
> אמינה מרחוק** — כפתור "מדוד" במסך הזה הוא מה שסוגר את הסעיף האחרון של v0.2.0.
> כלומר Settings לא רק מוסיפה פיצ'ר, היא **מסירה חסם** מגרסה פתוחה.


**מטרה:** שהמשתמש יכוונן את הקליינט שלו **בלי מפתח תוכנה**. היום כל שינוי של מספר
אחד — עוצמת ה‑bend, טווח הטמפו, רזולוציית הג'וג — הוא מחזור מלא של קוד → commit →
push → pull → הרצה מחדש. הסבב על `BEND_PER_TICK` ב‑30/08 לקח שלוש הלוך‑ושוב בשביל
להחליף `0.05` ב‑`0.1`. זה לא סביר, וזה ייחזור בכל בקר חדש.

**הדחיפות אמיתית ומוכחת:** ‏`JOG_TICKS_PER_REV` הוא **תכונה של החומרה**, לא של
התוכנה. הוא שונה בין בקרים, ולא הצלחנו למדוד אותו באמינות מרחוק (שלוש מדידות:
696 · 673 · 669). קבוע קשיח בקוד הוא התשובה הלא נכונה לשאלה הזאת מלכתחילה.

### ממצא שקדם לאיפיון: השמירה כבר מפוצלת

סקירה של הקוד הראתה ש**כבר יש שלוש הגדרות נשמרות, בשני מנגנונים שונים, בלי בעלים**:

| מה | איפה | מנגנון |
| --- | --- | --- |
| תיקיית הספרייה | `soundgrid:libraryDir` | `idb-keyval` |
| מיפוי MIDI מותאם | `soundgrid:midiMapping` | `idb-keyval` |
| סימון מפתח (Camelot/מוזיקלי) | `soundgrid:keyMode` | `localStorage`, בתוך `Library.tsx` |

אין סכימה, אין גרסה, אין מיגרציה, ואין קובץ אחד שאפשר לשאול "מה שמור". ‏v0.2.5
היא גם התיקון של זה — לא רק מסך חדש.

### מה ניתן לקינפוג, ומה בכוונה לא

**שכבה 1 — חובה. חומרה ותחושה, שונה בין אנשים:**
- `JOG_TICKS_PER_REV` — רזולוציית הג'וג. **עם כפתור "מדוד"** שמריץ את הספירה
  בתוך האפליקציה במקום לשלוח את המשתמש ל‑`midi-check.html`.
- `BEND_PER_TICK` · `KEYBOARD_BEND` — עוצמת הדחיפה, מהגלגל ומהמקלדת.
- `TEMPO_RANGE` — ‏±8% / ±16% / ±50%, בחירה סטנדרטית בכל תוכנת DJ.
- `BRAKE_SEC` · `SPIN_UP_SEC` — זמני עצירה והתנעה במצב ויניל.
- מכשיר פלט אודיו — **קיים כבר** ב‑`TopBar`, ומקומו כאן.

**שכבה 2 — כדאי. העדפות:**
- סימון המפתח (Camelot מול מוזיקלי) — קיים, מיושם ad‑hoc, יעבור לכאן.
- זום הוויב‑פורם (`PX_PER_SEC`).
- `EQ_DB` — עומק החיתוך של האקווליזר.
- `JOG_SMOOTHING` — כמה הסקראצ' "רך".
- ניהול מיפוי MIDI: ‏Learn, איפוס, ומה ממופה כרגע.
- תיקיית הספרייה.

**שכבה 3 — לא נחשוף, וזו החלטה ולא השמטה.** ‏`POSITION_EPSILON_SEC`,
‏`DECLICK_SEC`, `SILENT_BELOW_RATE`, `ANCHOR_EVERY_QUANTA`, `STANDSTILL`,
‏`HOLD_TIMEOUT_MS`, `JOG_REPORT_MS`, `QUIET`, `PLATTER_SIZE`.
אלה **כיולים שמקבעים באגים שכבר תוקנו**, לא העדפות. ‏`PLATTER_SIZE` היא הדוגמה
החדה: יש לה רצפה מוצהרת של 120px שנקנתה ב‑v0.2.3, וחשיפתה היא הזמנה לשבור אותה
שוב. ‏`POSITION_EPSILON_SEC` הוא תיקון לפלייהד קפוא. פקד למשתמש על מספר כזה הוא
דרך מנומסת לתת לו לשבור לעצמו את התוכנה.

### ארכיטקטורה

- **פורט אחד**: ממשק `Settings` ב‑`core/ports/`, עם `get`/`set`/`subscribe`
  וסכימה **ממוספרת גרסה**, ומיגרציה משלושת המפתחות הקיימים. המימוש שלו יושב
  תחת `platform/`, כמו כל שאר הפורטים.
  *(שם הקובץ בכוונה לא נקבע כאן: `doc-paths` בודקת שכל נתיב במסמכים קיים
  בפועל — ובצדק — ולתכנון אין דרך לומר "עוד לא נכתב". תכנון שמקבע שמות קבצים
  הוא ממילא תכנון יתר.)*
- **הערכים נכנסים ל‑`core/` כנתונים, לא כייבוא.** ‏`core/constants.ts` נשארת
  ברירת המחדל; מה שהמשתמש שינה עובר כפרמטר. טוהר `core/` לא נשבר.
- **זהירות ביצועים:** ‏`controls.ts` מקבל ~670 הודעות ג'וג לשנייה. קריאת הגדרה
  לכל הודעה היא בדיוק סוג הטעות שכבר עשינו עם חיווי ה‑jog — ההגדרות נקראות
  לתוך משתנה מקומי ומתעדכנות ב‑`subscribe`, לא נשאלות בכל טיק.
- **כלל הברזל חל:** ערך מחוץ לטווח **נאמר במסך** ולא נחתך בשקט. ליד כל שדה —
  ברירת המחדל וכפתור חזרה אליה, כי ערך שנשבר חייב להיות הפיך.

### מה זה לא

לא מרכז העדפות כללי, לא ערכות נושא, לא סנכרון לענן, ולא חשיפה של שכבה 3.
**כל שדה במסך חייב לענות על "איזו החלטה של המשתמש זה?"** — ואם התשובה היא
"אף אחת, זה כיול", הוא לא נכנס.

### הושלם כאשר

1. המשתמש משנה את עוצמת ה‑bend **במסך**, מרגיש את ההבדל מיד, וזה שורד רענון.
2. כפתור "מדוד" מחזיר `JOG_TICKS_PER_REV` מסיבוב אמיתי, בלי `midi-check.html`.
3. שלושת המפתחות הקיימים **עברו מיגרציה** — ההגדרות של משתמש קיים לא נעלמות.
4. אין ירידה ב‑FPS בזמן סיבוב ג'וג רציף (נמדד בדפדפן, לפני ואחרי).
5. בדיקה ב‑`tests/core/` על הסכימה והמיגרציה, ובדיקת ריפו שאף קבוע משכבה 3 לא
   דלף למסך.

### מה נבנה בפועל (30/08)

האיפיון מעל התקיים במלואו, ו**הורחב באישור המשתמש** אחרי השוואה מול צילומי מסך
ממערכת מסחרית. תשע תוספות נכנסו: מהירות פלטר 33⅓/45, גודל טקסט בספרייה, תקרת FPS,
ציור ברזולוציית המסך, התנהגות בטעינת טראק, נעילת דק מנגן, מסך קיצורי מקלדת, סעיף
"מה המחשב הזה תומך", וספרות ה‑BPM. שלוש קטגוריות **נדחו בשם** כי התשתית חסרה
(‏Sync/Quantize → beatgrid ב‑v0.3, ‏Auto Gain → ניתוח ב‑v0.3, סימון טראקים שנוגנו →
persistence ב‑v0.4), וארבע **לא ייכנסו לעולם** ונאמרות על המסך במפורש: טלמטריה,
סטרימינג, ספריית iTunes, ותוספים.

הפירוט, המדידות ו‑17 בדיקות הדפדפן: [`docs/handoff/v0.2.5.md`](docs/handoff/v0.2.5.md).

## v0.2.6 — פתיחה עם הספרייה כבר טעונה ✅
**נסגרה 02/09.** ‏18/18 ב‑`scripts/verify-library-boot.mjs` מול Chromium 1194,
ו‑12 בדיקות יחידה ב‑`tests/core/library-boot.test.ts`.

**מטרה:** לפתוח את SoundGrid ולראות את הספרייה **בלי ללחוץ על כלום**, כמו כל תוכנת
DJ מותקנת. היום כל רענון דורש בחירת תיקייה מחדש.

### המגבלה שקובעת את כל האיפיון
**דפדפן לא יכול לפתוח תיקייה בלי בחירה של המשתמש.** זה גבול אבטחה מכוון, לא חוסר
ב‑API: אילו דף היה יכול לקרוא את `Music` מיוזמתו, הוא היה יכול לקרוא כל תיקייה.
לכן "טעינה אוטומטית" כאן פירושה שני דברים שונים:

- **ביקור חוזר** — ה‑handle כבר שמור ב‑IndexedDB (`HANDLE_KEY` ב‑`library.ts`). אם
  ההרשאה עדיין `granted`, מותר לסרוק **בלי שום לחיצה ובלי דיאלוג**. זו הטעינה
  האוטומטית האמיתית, והיא המקרה הרווח.
- **ביקור ראשון** — לחיצה אחת היא המינימום התיאורטי. מה שכן אפשר: לפתוח את הדיאלוג
  **כבר בתוך תיקיית המוזיקה** של מערכת ההפעלה, דרך `startIn: 'music'`. היום
  `pickLibraryFolder` מעביר רק `mode` ו‑`id` — זו תוספת של מילה אחת.

בבניית הדסקטופ (Tauri, v1.0) המגבלה נעלמת וקוראים את `~/Music` ישירות. **לכן
הממשק כאן מתוכנן כך שהמעבר יהיה החלפת מימוש מאחורי ה‑port, ולא שינוי חוויה.**

### התנהגות
שלושה מצבים בעלייה, ותמיד אחד מהם — אף פעם לא רשימה ריקה בלי הסבר:

1. **יש תיקייה שמורה וההרשאה בתוקף** → סורקים מיד, בלי לחיצה. הספרייה פשוט שם.
2. **יש תיקייה שמורה אבל ההרשאה פגה** → **לא קופצים בדיאלוג.** הדפדפן ממילא חוסם
   בקשת הרשאה שלא הגיעה ממחווה, וניסיון כזה נכשל בשקט. במקום זה שורה שקטה שמחכה:
   «הספרייה `Tracks` — לחץ כדי להמשיך». לחיצה אחת → `requestPermission` → סריקה.
3. **אין תיקייה שמורה** (משתמש חדש) → כפתור יחיד «טען את תיקיית המוזיקה שלי», שפותח
   את הדיאלוג ישר בתוך Music.

**לא טוענים את `Music` עצמה כברירת מחדל.** אצל המשתמש הספרייה יושבת תחת
`Music/Tracks` — תת‑תיקייה של Music. טעינת Music כולה הייתה גוררת פנימה הקלטות
וקבצים שאינם סט, ומנפחת את הסריקה. Music הוא **נקודת הפתיחה של הדיאלוג**, לא הבחירה.

### לא בתכולה
- לא לסרוק כמה תיקיות במקביל, ולא crates (v0.8).
- לא מטמון ניתוחים — זה v0.4. כאן רק מיקום התיקייה נזכר; הסריקה עצמה רצה מחדש
  (0.6 שנייה ל‑360 קבצים, נמדד ב‑v0.1.7, אז זה לא מצדיק מטמון בגרסה הזו).

### אף פעם לא לדלג בשקט
- **התיקייה נמחקה או שמה שונה** → `NotFoundError`. להגיד את זה בשם:
  «`Tracks` כבר לא נמצאת» + כפתור לבחור מחדש. **לא** רשימה ריקה.
- **המשתמש בחר "אפשר רק הפעם"** ולא "בכל ביקור" → הטעינה האוטומטית לא תעבוד,
  וזה ייראה כמו באג. להסביר את זה במקום, בשורה של מצב 2.
- **הסריקה מצאה 0 קבצים ניגנים** → להגיד כמה קבצים נראו וכמה נדחו ולמה, לפי כלל
  ה‑badge הקיים מ‑`59c5fe5`.
- **`showDirectoryPicker` לא קיים** (לא Chromium) → `fileSystemAccessSupported()`
  כבר מטפל; לוודא שהמצב הזה נשאר גלוי ולא נבלע במסלול החדש.

### הושלם כאשר
- ביקור חוזר עם הרשאה בתוקף: הספרייה מוצגת **בלי אף לחיצה**, והשורה הראשונה
  מופיעה תוך פחות משנייה מרגע טעינת הדף.
- ביקור חוזר בלי הרשאה: **בדיוק לחיצה אחת** מחזירה את הספרייה.
- ביקור ראשון: הדיאלוג נפתח כשתיקיית המוזיקה של מערכת ההפעלה כבר מוצגת בו.
- תיקייה שנמחקה מייצרת הודעה **ששמה את שם התיקייה**, לא רשימה ריקה.
- אף מסלול לא מסתיים במסך ריק בלי משפט שמסביר מה קרה.

## v0.3.0 — Beatgrid & Phase Sync ✅
**נסגרה 02/09.** הרשומה המלאה — מה נבנה, מה נמדד (24 בדיקות ב‑`tests/core/
beatgrid.test.ts`, אימות דפדפן חי בכל תת‑צעד), ושני הבאגים שסקירה עצמאית תפסה
ותוקנו לפני הסגירה: [`docs/handoff/v0.3.0.md`](docs/handoff/v0.3.0.md).

**מטרה:** רשת ביט אמינה ו‑SYNC שמיישר גם פאזה, לא רק BPM.
- זיהוי downbeat ראשוני, שמירת beatgrid (offset + BPM) לטראק
- עריכת רשת ידנית: הזזה, half/double, "set downbeat here", tap‑tempo
- Phase‑align ב‑SYNC: יישור הביט הקרוב לדק המאסטר
- Quantize ל‑hot cues, loops, ו‑beat‑jump — **וגם לנקודת ה‑CUE הראשית.**
  המשתמש דיווח (30/08) ש‑CUE נחתם באמצע בית. זה נכון והוא צודק: `cue()`
  שומר את `deck.position` המדויק, ובלי רשת ביט אין למה להצמיד. הסעיף הזה
  הוא התשובה — לא באג נפרד
- בורר "master deck" ידני/אוטומטי
- **הושלם כאשר:** שני טראקים באותו BPM נשארים בפאזה אחרי SYNC לאורך 2 דקות.

## v0.3.1 — מספר גרסה בקליינט ✅
**נסגרה 03/09.** הרשומה המלאה — מה נבנה, מה נמדד: [`docs/handoff/v0.3.1.md`](docs/handoff/v0.3.1.md).

## v0.3.2 — עמודת ז'אנר ✅
**נסגרה 02/09.** ‏9/9 ב‑`tests/core/genres.test.ts`, `npm run check` ירוק, בנייה
מלאה (`npm run build`) עברה, ובדיקת עשן ב‑Chromium (`playwright-core`, זמני,
לא נשמר) — האפליקציה עולה בלי שגיאת קונסולה יחידה עם הייבואים החדשים. נבנתה
כ‑`v0.2.10` על main ישן ומוספרת מחדש אחרי ש‑v0.3.0 נסגר תוך כדי העבודה (ר' `HANDOFF.md`).
**הסריקה האמיתית מול `Tracks` נשארה לבדיקת הבעלים** — אין File System Access
ואין הספרייה האמיתית בסביבת הסוכן המרוחקת.

**מטרה:** עמודת Genre בטבלת הספרייה, מימין ל‑Artist, מאוכלסת אוטומטית משם תיקיית
האב בזמן הסריקה, ניתנת לעריכה ידנית מרשימה סגורה. הספק המלא והתכנון:
`workshop-output/FEATURE_SPEC.md` ו‑`workshop-output/PLAN.md` (נכתבו ואושרו
ב‑02/09, אחרי סבב סקירות מוצר/ארכיטקטורה/עיצוב/QA).

### התנהגות
- ‏`core/genres.ts` — רשימת ז'אנרים סגורה וקבועה + טבלת כינויים קטנה, התאמה
  מנורמלת (אותיות/מפרידים) ולא fuzzy — כדי לא לסכן שיוך שגוי בשקט. `matchGenre`
  ו‑`parentFolderName` שתיהן טהורות ונבדקות ישירות.
- ‏`scanLibrary` (`platform/source-fsaccess/library.ts`) גוזר את הז'אנר מתיקיית
  האב הישירה של כל קובץ, ומחזיר `unrecognizedGenre: Record<folder, count>` —
  אותה צורה בדיוק כמו `skipped` הקיים.
- תיקייה לא‑מזוהה (**כולל `Final`**, שאינה מקבלת יחס מיוחד) נספרת ב‑badge שלישי
  בכותרת הספרייה, באותו סגנון כמו "N skipped"/"N unreadable", עם tooltip לפי
  שם תיקייה. טראק מ‑"+ Files" (בלי תיקיית אב) מקבל "–" ולא נספר.
- עריכה ידנית — `<select>` נחשף ב‑hover על התא (מחקה את toggle ה‑Key‑mode
  הקיים), מוגבל לרשימת `GENRES` בלבד. עובר דרך `controls.ts:setTrackGenre`
  (נקודת המעבר היחידה לפעולת המשתמש) ונשמר ב‑`platform/genre-overrides-idb/`
  — מאגר IndexedDB קטן וייעודי, לא הפורט `Persistence` הכללי שעדיין לא מומש.
  עריכה שורדת סריקה חוזרת של אותו קובץ; כישלון שמירה משאיר את הערך על המסך
  ומציג הודעה (`NoticeSource: 'library'`), לא נבלע.
- תיבת הסינון הקיימת (`controls.ts:filteredTracks`) מחפשת גם לפי ז'אנר.

### לא בתכולה
- המלצות מיקס לא נעשות ז'אנר‑מודעות — `core/recommend.ts` נשאר BPM/key בלבד.
- אין מיון לחיצה על עמודות (לא קיים היום בכלל, לא נוסף כאן).
- אין טקסט חופשי לז'אנר — רשימה סגורה בלבד, לפי החלטת המשתמש.

### אף פעם לא לדלג בשקט
- תיקייה לא‑מזוהה → badge בכותרת, לא עמודה ריקה בלי הסבר.
- כישלון כתיבה ל‑IndexedDB בעריכה ידנית → הודעה על המסך, לא באג שקט שנראה כמו
  "השינוי לא נשמר בלי שום סימן".

### חוב שנפתח כאן, לא תוקן
מזהה הטראק בנוי מהנתיב היחסי. עריכת ז'אנר ידנית ואז העברת הקובץ לתיקיית ז'אנר
אחרת לפני סריקה חוזרת — העריכה אובדת בשקט (מזהה חדש, אין קישור לישן). המשתמש
קיבל את זה כחוב ידוע במפורש (02/09); ר' `HANDOFF.md`.

### הושלם כאשר
- סריקה של `Tracks` האמיתית משייכת נכון HipHop/House/Techno/Trance/Mizrahi
  בלי התערבות, ו‑`Final` נספר ב‑badge.
- עריכה ידנית שורדת סריקה חוזרת של אותה תיקייה, קבצים לא זזו.

## v0.4.0 — ניתוח מתמשך + מטא‑דאטה קבועה ✅
**מטרה:** לנתח פעם אחת, לזכור לתמיד. תוצאה מלאה: [`docs/handoff/v0.4.0.md`](docs/handoff/v0.4.0.md).
- Web Worker לניתוח ברקע (decode, peaks, BPM, beatgrid) עם תור ומגבלת concurrency
- מטמון ב‑IndexedDB לפי hash קובץ: peaks, BPM, grid, duration, cue points
- אינדיקציית "מנותח / בתור / נכשל" בספרייה, ניתוח באצווה של תיקייה שלמה
- טעינת דק מיידית ממטמון (בלי המתנה לניתוח)
- **לחבר את `onLoadPlayhead` מ‑v0.2.5 ולהסיר את הכתובית `pending` שלו.** השדה
  "מהתחלה / מנקודת ה‑Cue הראשונה" קיים במסך ההגדרות מאז v0.2.5 ואינו יכול לפעול, כי
  אין cue נשמר. ברגע שה‑cue points במטמון — הוא נעשה אמיתי. **בלי זה, v0.4 משאירה
  אחריה פקד שמצהיר שהוא לא עובד בזמן שהנתונים כבר קיימים** — וזה גרוע יותר מהמצב היום.
- **הושלם כאשר:** טעינה שנייה של אותו טראק היא < 200ms ללא ניתוח מחדש, **ו‑
  `onLoadPlayhead` מזיז את הפלייהד לנקודת ה‑Cue השמורה.**

## v0.4.5 — סימון "השיר הבא" ✅
**נסגרה 2026-09-04.** התכולה המלאה ומה שנמדד: [`docs/handoff/v0.4.5.md`](docs/handoff/v0.4.5.md).

## v0.4.7 — קרבת אנרגיה (מיקס)
**נדחה מ‑v0.4.5** (2026-09-04) — ארבע הסקירות (מוצר/ארכיטקטורה/עיצוב/QA) המליצו על
כך פה אחד: אין היום מדד אנרגיה ברמת טראק, ואין אפילו הגדרה למה זה אומר (RMS?
ספקטרלי? loudness נתפס?) — דורש מעבר ניתוח חדש, גרסת מטמון חדשה, וניקוד/UI
חדשים. גודל של גרסה בפני עצמה, לא תוספת ל‑v0.4.5 או ל‑v0.4.6.
**אין כאן איפיון עדיין** — סבב מוצר/ארכיטקטורה משלה כשמגיעים אליה.

## v0.4.8 — Hint mode ✅
**נסגרה 2026-09-04.** התכולה המלאה ומה שנמדד: [`docs/handoff/v0.4.8.md`](docs/handoff/v0.4.8.md).

## v0.4.6 — Mix Assist: הצעת מיקס ומעבר אוטונומי אחד ✅
**נסגרה 2026-09-05.** התכולה המלאה ומה שנמדד: [`docs/handoff/v0.4.6.md`](docs/handoff/v0.4.6.md).

### פתוח לגרסה עתידית: דיוק ומהירות ההצטרפות במעבר האוטונומי
נמדד בפועל על הספרייה האמיתית (v0.4.6 שלב 8, 05/09, 10 זוגות תואמי-BPM): שגיאת
פאזה ב"הצטרפות" (`atJoin`) חצתה 1ms-epsilon ב‑7/10 זוגות (עד 280ms), אבל תוך 3
שניות לולאת ה‑SYNC כיווצה כמעט את כולן לכמעט אפס — חוץ מזוג אחד שנשאר על ~100ms
(ל‑beat grid שם היה `unconfirmed`). שני כיוונים לבדוק בגרסה שתטפל בזה:
- **לצמצם את פער ה‑`atJoin` עצמו**, לא רק לסמוך על ה‑SYNC לתקן אחר כך — ייתכן
  שהפער נובע מפער זמן אמיתי בין החישוב (`phaseAlignedEntrySec`) לרגע ה‑seek
  בפועל, לא מהמתמטיקה עצמה; דורש מדידה ישירה בתוך `controls.ts`, לא רק חיצונית.
- **מעבר לדק עם beat grid `unconfirmed`** — האם `startAutoTransition` צריך לסרב
  או להזהיר, כמו שהוא כבר מסרב כשאין grid בכלל?
המספרים המלאים: `scripts/verify-mix-assist-transition-quality.mjs`.

## v0.5.0 — Pad Modes
**מטרה:** גריד הפדים עושה יותר מ‑hot cues.
- מצבים: Hot Cue, Auto Loop, Loop Roll, Beat Jump, Sampler (מעבר במצב + shift)
- שכבת shift גלובלית לפקדים (כפתור SHIFT בקונטרולר / מקש)
- אינדיקציה ויזואלית של המצב הפעיל לכל דק, צבעי פדים לפי מצב
- מיפוי FLX4: כפתורי בחירת מצב הפדים (Hot Cue / Pad FX / Beat Jump / Sampler)
- **הושלם כאשר:** אפשר להחליף מצב פדים מהקונטרולר וכל מצב מפעיל את הפעולה הנכונה.

## v0.5.5 — שליטה בשפה טבעית / קול
**מטרה:** לומר/להקליד "לופ על הדרופ 8 תיבות", "תכניס אקפלה מדק B", "פילטר סוויפ
לתוך הברייקדאון" — וזה קורה. אפשרי רק אחרי ש‑`controls.ts` בשל (v0.5). ראה
`docs/architecture/directions.md` §4.
- שכבת translate: טקסט → `ControlAction[]` דרך `AIProvider` (tool‑calling מול
  קטלוג הפעולות של `controls.ts`). **לא עוקפת את ה‑choke point**
- `AIProvider` model‑agnostic: מקומי (WebGPU/WASM) · מפתח API של המשתמש · self‑hosted
- קלט קול דרך Web Speech API (אופציונלי, מאחורי capability)
- הכל אופציונלי — כבוי כברירת מחדל, האפליקציה עובדת מלא בלעדיו
- **הושלם כאשר:** 10 פקודות נפוצות מתורגמות נכון לפעולות; פקודה לא ברורה מחזירה
  בקשת הבהרה ולא פעולה שגויה.

## v0.6.0 — Sampler / Performance Decks
**מטרה:** בנק סאמפלים שמתנגן בסנכרון.
- בנק של 8–16 slots, טעינה מהספרייה או הקלטה מהמאסטר
- לכל slot: gain, מצב (one‑shot / loop / gated), sync ל‑BPM המאסטר, trigger מ‑pad
- ערוץ sampler נפרד במיקסר (volume + לכיוון master/cue)
- שמירת בנקים ל‑IndexedDB, ייצוא/ייבוא בנק
- **הושלם כאשר:** הפעלת slot ב‑loop נשארת בסנכרון עם דק שמנגן.

## v0.7.0 — FX Units
**מטרה:** אפקטים מסונכרני‑ביט.
- שני FX racks, כל אחד: בחירת אפקט, wet/dry, זמן (1/4…4 beats), on/off
- אפקטים: Delay, Echo, Reverb, Filter (LP/HP), Flanger, Phaser, Bit Crusher, Roll
- ניתוב FX: לכל ערוץ / מאסטר / send
- מיפוי FX ל‑FLX4 (beat knob, לחצני on, paddle) ול‑MIDI Learn
- מימוש דרך גרף Web Audio; אפקטים כבדים כ‑`AudioWorklet`
- **הושלם כאשר:** Delay 1/4 על דק ב‑128 BPM מתיישר לביט בשמיעה.

## v0.8.0 — ניהול ספרייה
**מטרה:** ספרייה שאפשר לעבוד איתה על אלפי טראקים.
- Crates / playlists (עץ), גרירת טראקים לתוכם, crates חכמים (סינון שמור)
- עמודות: אמן, שם, BPM, key, זמן, דירוג, הערה, נגן לאחרונה — מיון וסינון
- פרסינג תגיות ID3 / Vorbis / MP4 (אמן/כותרת/אלבום/artwork)
- History / session log של מה שנוגן, ייצוא רשימת סט
- חיפוש מהיר עם אינדקס (שם/אמן/BPM range/key)
- **הושלם כאשר:** ספרייה של 5,000 טראקים נגללת ומסתננת ב‑60fps.

## v0.8.5 — חיפוש סמנטי בספרייה
**מטרה:** "פסייטראנס 138 פיק‑טיים שמתמזג מזה", "טראקים שנשמעים כמו זה" — מעבר לתגיות.
- audio embeddings דרך `Analyzer` (מודל on‑device: CLAP‑style / MFCC+ML), נשמרים
  ב‑`AnalysisCache` לפי hash תוכן, ניידים בין מכשירים
- חיפוש קרבה (cosine) + סינון היברידי עם המטא‑דאטה הקיים
- "more like this" מטראק נבחר או מהדק המנגן
- הכל מקומי; אין שליחת אודיו לרשת
- **הושלם כאשר:** "more like this" על טראק מחזיר 10 תוצאות סבירות מספרייה של 1000+.

## v0.9.0 — זיהוי סולם + מיקס הרמוני
**מטרה:** לדעת את הסולם ולמזג לפיו.
- זיהוי key (chroma + template matching), תצוגת Camelot + מוזיקלי
- Key‑lock (master tempo) — שינוי tempo בלי שינוי pitch, ב‑`AudioWorklet`. מועמד קונקרטי:
  פורט WASM של [Rubber Band Library](https://github.com/breakfastquay/rubberband) (LGPL,
  time‑stretch/pitch‑shift באיכות גבוהה, זה מה שרוב תוכנות ה‑DJ בקוד פתוח משתמשות בו),
  מול phase vocoder עצמאי בקוד שלנו — עדיין **החלטה פתוחה** (ראה `HANDOFF.md`), לא
  ברירת מחדל. לבדוק רישיון LGPL מול חבילה סטטית ל‑SPA לפני שסוגרים
- Key‑sync: התאמת pitch של דק לדק השני
- הדגשה בספרייה של טראקים תואמים הרמונית לדק המנגן
- **הושלם כאשר:** key‑lock ב‑±8% לא משנה גובה צליל בשמיעה; זיהוי key נכון ב‑>70% במדגם.

## v0.9.5 — מצב קואצ'ינג
**מטרה:** ביקורת AI על הביצוע — משהו שאין לו מקבילה טובה ב‑Serato/rekordbox. ענק
ל‑DJ שלומד. ראה `docs/architecture/directions.md` §4–§5.
- מדדים אובייקטיביים (בלי AI): beat drift בין הדקים, התנגשות EQ (אנרגיה חופפת
  בבנד), קפיצות gain, אורך מעבר, clipping על המאסטר
- שכבת AI: סיכום מילולי אחרי הסט ("המעבר ב‑12:30 היה חד; ה‑bass של שני הדקים רץ
  יחד 8 שניות") + נדנוד עדין בזמן אמת (אופציונלי, כבוי כברירת מחדל)
- session log מוקלט (פעולות + מדדים) → ניתן לצפייה חוזרת ולייצוא
- **הושלם כאשר:** אחרי סט של 20 דק' מתקבל דו"ח עם 3+ הערות ממוקדות‑זמן ומדדים.

## v0.10.0 — הקלטה + שידור
**מטרה:** להקליט את הסט ולשדר.
> **הערה מהבראנץ׳ המקביל (יובאה ב‑v0.2.8):** שיחה אחרת איפיינה את ההקלטה
> וכתבה «WAV דורש מקודד משלנו — לא בגרסה הזו». **זה מדויק לגבי `MediaRecorder`
> ומטעה לגבי WAV.** ‏`MediaRecorder` בכרום פולט WebM/Opus בלבד, אבל WAV מ‑PCM
> אינו קידוד — הוא כותרת של 44 בתים לפני הדגימות, פחות מ‑50 שורות מול
> `AudioWorklet`. **החלטת המשתמש (02/09) עומדת: WAV קודם.**
>
> מה שכן נכון באיפיון ההוא ונלקח: ההקלטה היא **ענף ולא שרשרת** — אם היא נופלת,
> הסט ממשיך לנגן; לימיטר לפני ההקלטה בלבד; ביטול דיאלוג השמירה **לא** מוחק את
> ההקלטה בשקט (היא נשארת בזיכרון עם שורה «לא נשמרה — שמור או מחק»); ונגמר
> הזיכרון נאמר בשם, עם כמה דקות כן נשמרו.
>
> **והפער שהאיפיון ההוא צדק שצריך לנקוב בו:** `CLAUDE.md` אומר «קוראים את
> הקבצים של המשתמש, לעולם לא כותבים». הקלטה **חייבת** לכתוב. הכלל אינו נשבר —
> הוא נוגע לקבצים שהמשתמש כבר מחזיק; הקלטה נכתבת ל**קובץ חדש** שהוא בוחר
> בדיאלוג. לנסח את זה ב‑`CLAUDE.md` **לפני** שכותבים שורת קוד ראשונה של v0.10.

- **‏WAV קודם — החלטת המשתמש (02/09).** הקלטת המאסטר ל‑WAV נכתבת ראשונה
  ומשוחררת לבד; ‏FLAC/OGG ושידור באים אחריה. ‏WAV הוא הפורמט היחיד שאפשר לכתוב
  מ‑PCM בלי encoder חיצוני, והיחיד שנכנס לעריכה בלי אובדן — אז הוא גם המהיר
  לשחרור וגם הנכון לארכיון. מד זמן וגודל בזמן ההקלטה
- חלוקה לקבצים לפי טראק (cue markers), ייצוא cue sheet
- שידור אופציונלי ל‑Icecast/SHOUTcast (WebSocket/HTTP), בקרת bitrate
- הגנת clipping — לימיטר על המאסטר לפני ההקלטה
- **הושלם כאשר:** הקלטה של 30 דק' יוצאת ללא drift וללא clipping.

## v0.11.0 — עורך מיפוי חומרה + LED Feedback
**מטרה:** להתאים כל קונטרולר, לא רק FLX4.
- UI ויזואלי למיפוי: בחר פעולה → הזז פקד → נשמר
- פריסטים מרובים, ייבוא/ייצוא JSON, שיתוף פריסטים
- MIDI output — הדלקת LED/טבעות לפי מצב (play, cue, loop, pad)
- טיפול ב‑14‑bit faders, soft‑takeover לפקדים אבסולוטיים
- פריסטים נוספים: DDJ‑FLX2, DDJ‑REV1, generic 2‑channel
- **הושלם כאשר:** משתמש ממפה קונטרולר לא מוכר במלואו בלי לגעת בקוד.

## v0.11.5 — Plugin API
**מטרה:** SoundGrid extensible — משהו ש‑Serato/rekordbox לא באמת מאפשרים.
- `controls.ts` נחשף כ‑API ציבורי יציב + מודל אירועים (state, ControlAction log, clock)
- plugins בארגז חול: pad modes מותאמים, FX (Web Audio graph), סקריפטים, פאנלים ב‑UI
- מניפסט + הרשאות מפורשות; טעינה מ‑URL / קובץ מקומי
- דוגמאות ראשונות: pad mode "beat repeat", FX "gater", פאנל "set notes"
- **הושלם כאשר:** plugin חיצוני מוסיף pad mode עובד בלי build מחדש של הליבה.

## v0.12.0 — שדרוג Waveform
**מטרה:** waveform ברמת Serato.
- Overview מלא של הטראק + waveform מזוום, גרירה על ה‑overview לניווט
- צביעת RGB לפי תדר (פיצול 3 בנדים → low=אדום, mid=ירוק, high=כחול)
- רינדור ב‑`OffscreenCanvas` / WebGL לביצועים
- סימוני cue/loop/grid על שני התצוגות, "needle drop" מדויק
- מצב waveform אנכי / אופקי, waveform תואם לזוג דקים (stacked)
- **סימון תיבות על ה‑waveform, בסגנון Serato/rekordbox (הוגדר 04/09):** קו דק על כל
  פעימה, קו גבוה וצבעוני על כל פעימה רביעית (= תחילת תיבה) — לא סימון "פרייז" (כל 4
  תיבות), רק רמת התיבה הבודדת. **תלוי בזיהוי `downbeat` אוטומטי אמיתי** — החוב הטכני
  הקיים (`HANDOFF.md`): `estimateBeatGrid` (`core/beatgrid.ts`) מוצא פעימה, לא יודע
  להבדיל פעימה 1 של תיבה משאר השלוש בלי ניתוח טימבר (קיק מול סנר), וה‑`downbeat` היחיד
  שקיים היום הוא הכפתור הידני "Set downbeat here" ב‑`BeatGridPanel`. **החלטת המשתמש
  (04/09): לא להתחיל בסימון הזה על בסיס downbeat ידני או דיפולט לפעימה 0 — לחכות
  לזיהוי אוטומטי אמיתי קודם**, גם אם זה דוחה את התכולה הזאת מעבר לשאר v0.12.0.
- **הושלם כאשר:** שני waveforms + overview רצים יחד ב‑60fps.

## v0.13.0 — Auto‑Mix / מעברים חכמים
**מטרה:** תור שמתנגן לבד עם מעברים סבירים.
- תור השמעה (drag מהספרייה), auto‑load לדק הפנוי
- זיהוי intro/outro, crossfade אוטומטי בנקודה הנכונה עם beat‑match
- אסטרטגיות מעבר: cut, fade, EQ blend; משך מתכוונן
- סידור תור לפי אנרגיה / key / BPM proximity
- **הושלם כאשר:** 5 טראקים בתור מתנגנים רצף שעה ללא התערבות ובלי train‑wreck.

## v0.13.5 — קו‑פיילוט מיקס חי
**מטרה:** לא auto‑mix — עוזר שממשיך לתת לך לנגן, אבל מייעץ בזמן אמת. ה‑moat
האמיתי מול Serato/rekordbox. ראה `docs/architecture/directions.md` §4.
- הצעת "השיר הבא" חכמה (מעל v0.4.5): מדרג את התור/הספרייה לפי התאמה לדק המנגן +
  קשת אנרגיה של הסט עד עכשיו
- אזהרות חיות: "התנגשות הרמונית", "שני basslines רצים יחד", "האנרגיה יורדת 3 טראקים"
- הצעת נקודת מעבר ("הבא את דק B ב‑phrase הבא, ~0:24 מכאן")
- הכל הצעות בלבד — דרך `controls.ts`, המשתמש מחליט. אופציונלי, כבוי כברירת מחדל
- **הושלם כאשר:** במהלך סט חי הקו‑פיילוט מציע טראק הבא סביר ומזהה לפחות סוג אחד
  של train‑wreck לפני שהוא קורה.

## v0.14.0 — מצב 4 דקים
**מטרה:** C ו‑D.
- דקים C/D עם אותו מנוע, layout 2×2 או deck‑focus
- "deck switch" בקונטרולר 2‑ערוצי (כפתור shift מחליף בין A/C ו‑B/D)
- מיקסר 4 ערוצים, crossfader assign (thru / A / B) לכל ערוץ
- **הושלם כאשר:** 4 טראקים מנגנים בו‑זמנית עם ניתוב crossfader נכון.

## v0.15.0 — מקורות ענן / סטרימינג
**מטרה:** לא רק קבצים מקומיים.
- שכבת מקור מופשטת (`TrackSource`): local FS, URL, ספק מקושר
- אינטגרציה עם שירותי סטרימינג לפי מה שה‑TOS מתיר (OAuth של המשתמש, ניגון דרך ה‑SDK הרשמי)
- Locker: קבצים שהמשתמש מעלה ל‑storage שלו, cache אופליין (Cache API)
- טיפול ב‑buffering / התנתקות באמצע טראק
- **הושלם כאשר:** אפשר לטעון לדק טראק ממקור מקושר ולנגן אותו יציב.

## v0.16.0 — Cue/Loop שמורים + ייבוא תגיות
**מטרה:** נקודות שמורות לכל טראק.
- Memory cues + saved loops לכל טראק ב‑IndexedDB, קפיצה מהירה ביניהם
- Loop rolls, saved loops עם שם, beat‑jump מתוך cue
- ייבוא read‑only של beatgrid/cues מקבצי המשתמש (GEOB/Serato Markers, rekordbox XML) — רק מהקבצים שלו
- ייצוא ה‑crates/cues של SoundGrid ל‑JSON
- **הושלם כאשר:** טעינת טראק מציגה את כל ה‑cues השמורים; ייבוא XML של rekordbox עובד.

## v0.17.0 — ליטוש UI + Layouts + מגע
**מטרה:** ממשק ביצוע רציני.
- פאנלים ניתנים לשינוי גודל/הסתרה, layout presets (2‑deck / 4‑deck / library‑focus)
- מצב ביצוע מסך‑מלא, deck skins / themes
- תמיכת מגע (טאבלט): פדים גדולים, גלילת ספרייה, ג'וג וירטואלי
- קיצורי מקלדת מלאים + עורך קיצורים
- נגישות: ARIA לכל הפקדים, ניווט מקלדת, ניגודיות
- **שפת ממשק: טקסט בעברית, הפריסה נשארת LTR — החלטת המשתמש (02/09).**
  תרגום מחרוזות בלבד: **אין** `dir="rtl"` ו**אין** היפוך פאנלים. דק A שמאל,
  דק B ימין — כמו על ה‑FLX4 שהיד יושבת עליו, וכמו הפריסה המכוילת ל‑1536×710.
  היפוך היה מנתק את הממשק מהחומרה ומהכיול גם יחד
- **הושלם כאשר:** אפשר לנהל סט שלם על טאבלט בלי עכבר.

## v0.18.0 — Latency ואיכות אודיו
**מטרה:** צליל הדוק ונקי.
- מעבר המנוע ל‑`AudioWorklet` (mixing, scratch, key‑lock ב‑worklet thread)
- בחירת buffer size / latency hint, מדידת latency בפועל והצגה
- הנחיות ASIO / WASAPI exclusive ב‑Windows, טיפול ב‑sample‑rate mismatch
- לימיטר + soft‑clip על המאסטר, מד VU/peak אמיתי, gain staging
- Dithering בהקלטה
- **הושלם כאשר:** round‑trip latency < 15ms על FLX4 עם דרייבר; אין xruns בסט של שעה.

## v0.19.0 — MIDI Clock / Ableton Link
**מטרה:** סנכרון עם ציוד חיצוני.
- שליחה/קבלה של MIDI clock (24 ppqn), start/stop
- Ableton Link (session tempo, phase) — דרך WASM port או שרת גשר מקומי
- SoundGrid כ‑master או slave, הצגת peers
- **הושלם כאשר:** דראם מכונה חיצונית נשארת נעולה ל‑BPM של SoundGrid.

## v0.20.0 — Stems + הפרדה בזמן אמת
**מטרה:** לשלוט בתופים/בס/ווקאל/מלודיה בנפרד.
- מודל הפרדת stems on‑device (WASM / WebGPU), ניתוח מראש ל‑4 ערוצי stem
- פאדרי stem + mute/solo לכל דק, מיפוי לקונטרולר
- פדים: acapella / instrumental / drums‑only בלחיצה
- Fallback: אם אין GPU — הפרדה offline בלבד עם מטמון
- **הושלם כאשר:** mute של ערוץ ה‑vocal בזמן ניגון נשמע נקי וללא latency מורגש.

## v1.0.0 — יציבות ושחרור
**מטרה:** מוצר שאפשר לתת לאנשים.
- PWA — התקנה, עבודה אופליין, service worker
- שמירת כל ההגדרות (audio, MIDI, layout, themes) + פרופילים
- Onboarding: בחירת קונטרולר, בחירת פלט, סריקת ספרייה — wizard
- חבילת טסטים: unit למנוע האודיו/analyze, e2e לזרימות מפתח, CI
- תיעוד משתמש + תיעוד מיפוי, דף נחיתה
- אופציונלי: build דסקטופ ב‑Tauri לגישת אודיו נייטיב (ASIO)
- **הושלם כאשר:** משתמש חדש מתקין, מחבר FLX4, ומנגן סט תוך 5 דקות בלי עזרה.

---

## רעיונות ל‑post‑1.0 (לא ממוספר)
- שיתוף פעולה לייב (שני DJ, WebRTC), video mixing, ניתוח sentiment של קהל,
  MIDI mapping marketplace, ענן sync של הספרייה בין מכשירים, גרסת מובייל מלאה.
