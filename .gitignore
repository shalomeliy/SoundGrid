# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# CodeGraph (index db is git-ignored inside .codegraph/)

# Claude Code — machine-local permissions/skill overrides (settings.json is shared)
.claude/settings.local.json
# Claude Code — ephemeral per-agent worktrees (isolation:"worktree" subagents)
.claude/worktrees/
graphify-out/
