# v0.8.7 — פרטיות/תנאי שימוש כנים + תיקון נגישות אמיתי ✅ (2026-09-14)

**נסגרה.** `npm run check` ירוק (מלבד 8 כשלים pre-existing ב‑`doc-commits.test.ts`,
שיבוט git רדוד, לא קשור). תוכננה כ‑`v0.8.4` בטיוטת הספסיפיקציה (12/09,
write-feature-spec: product/architecture/design/QA — security-expert לא
הופעל, אין שרת/API חדש ואין נגיעה בקבצי הספרייה); נבנתה בפועל בתור `v0.8.7`
כי `package.json` כבר עמד על `0.8.6` כש‑v0.8.6 קפץ בתור וננעל קודם.

> ארכיון. ההווה נמצא ב‑[`HANDOFF.md`](../../HANDOFF.md).

## מטרה

בקשת שלום המקורית כללה מדיניות קוקיז, החזרות, אישור טפסים, אנליטיקס, צדדים
שלישיים וביקורות — אף אחד מאלה לא קיים ב‑SoundGrid (מאומת ב‑grep טרי ל‑
`fetch(`/`analytics`/`cookie` ב‑`src/`, אפס תוצאות אמיתיות מלבד דף הפרטיות
עצמו שמדבר עליהן). לבנות דף שטוען אחרת היה הצהרה כוזבת — ההפך מהגנה. המטרה
בפועל: **גילוי נאות כן** (מה שכן קורה, ומה שבמפורש לא) + **תיקון נגישות
אמיתי** שהאודיט מצא.

## מה נבנה

1. **טאב "Privacy & Terms" חדש ב‑Settings** (`Settings.tsx` — `GROUPS` +
   `LegalPanel`, תוכן ב‑`src/app/legal-content.ts` נפרד כדי לא לנפח את
   `Settings.tsx`). חמישה כרטיסים באותו סגנון כמו `FieldRow`/`SystemPanel`:
   - **Privacy** — מה נשמר מקומית (הרשאת תיקייה, מטמון מטא‑דאטה, הגדרות)
     ולמה, ורשימה נקובה במפורש של מה שלא רלוונטי (קוקיז/אנליטיקס/צד‑ג׳/
     החזרות/הסכמת‑טפסים) — לא מושמט בשקט, אותה שיטה כמו `COMPANION_EXT`.
   - **Terms of use** — as-is, בלי אחריות, שימוש אישי, פטור מכשל בהופעה חיה.
   - **Trademarks** — לא בשיתוף/באישור AlphaTheta/Pioneer DJ/Serato/rekordbox.
   - **Copyright** — האחריות על זכויות בתוכן שהמשתמש מנגן/מקליט היא שלו.
   - **License** — קישור ל‑`LICENSE` (MIT) + `<details>` מקופל עם 257 קרדיטי
     קוד פתוח, נוצר מ‑`package-lock.json` ב‑`scripts/generate-oss-notices.mjs`
     (חד‑פעמי, לא נאכף ב‑`npm run check` — architecture-expert: עלות תחזוקה
     מול תועלת חדה). התוצר: `src/app/oss-notices.generated.ts`.
2. **שורה קבועה ב‑TopBar** ("Not affiliated with Pioneer DJ/Serato · Privacy")
   שפותחת את הטאב ישירות (`SettingsScreen` מקבל `initialGroup`) — לא קבורה
   בטאב שנפתח רק כשיש בעיה (product-expert). בלוק "What SoundGrid does not
   do" הקיים ב‑System מפנה לטאב המלא במקום לשכפל טענה שעלולה לסטות ממנו.
3. **`LICENSE` (MIT) בשורש** + `package.json` מקבל `"license": "MIT"`.
4. **`controls.ts` מקבל `selectTrack(id)`** — `Library.tsx` כתב `selectedId`
   ישירות לסטור מ‑`onClick`, ההפרה היחידה בקובץ הזה של "כל פעולת משתמש עוברת
   דרך `controls.ts`". תוקן תוך כדי חיווט המקלדת (הפונקציה נזקקה גם ל‑
   `onKeyDown`, אז לא הגיוני להשאיר שני נתיבים).
5. **שורת טראק ב‑Library נגישה למקלדת** — הייתה `onClick`/`onDoubleClick`
   בלבד, בלי `tabIndex`/`onKeyDown` (נוגד את "לעולם לא לשבור את נתיב
   המקלדת" של הפרויקט עצמו). עכשיו `tabIndex={0}` + Enter/Space קוראים
   ל‑`selectTrack` — **בחירה בלבד, לא טעינה לדק** (איזה דק היא שאלת מוצר
   נפרדת שלא הוכרעה).

## ממצא אמיתי תוך כדי אימות (לא רק בדיקה שנוספה)

בכתיבת `scripts/verify-privacy-a11y.mjs` (Playwright אמיתי מול `npm run dev`,
Chromium 1194) התגלה **שטבעת הפוקוס נעלמת בכל רכיב שמערבב `outline-none` עם
`focus-visible:outline-*`**. מוסכמת ה‑Tailwind הזו נשענת על משתנה CSS משותף
(`--tw-outline-style`), ו‑`outline-none` קובע אותו ל‑`none` **בלי שום תנאי**
— לא רק במצב "לא פוקוס". אז גם כש‑`:focus-visible` תפס (אומת ישירות עם
`el.matches(':focus-visible')` — `true`), ה‑`outline-style` בפועל נשאר
`none` בכל רכיב שנשא את שתי המחלקות יחד: `HintIcon`, וה‑`<select>`ים ב‑
Settings/TopBar/AiControlBar. שורת הטבלה ב‑`Library.tsx` תוקנה עם תוספת
חד‑שורתית (`focus-visible:[--tw-outline-style:solid]`) שדורסת את המשתנה
בחזרה בזמן פוקוס; שאר המקומות נשארו בכוונה בחוץ — תיקון רוחבי מעבר להיקף
הגרסה הזו, נרשם ב‑`HANDOFF.md`. **`Button` לא נפגע** — הטענה הראשונה שלי
כללה אותו בטעות; אין לו `outline-none` בכלל (`controls.tsx` — `base` של
`Button` לעולם לא כולל את המחלקה), ו‑`change-reviewer`'s תפס את זה בבדיקה
ישירה עם Tab אמיתי + `getComputedStyle` לפני שהתיעוד יצא לדרך עם טענה
גורפת שגויה.

## מה נמדד

- `npm run check` ירוק, כולל שתי בדיקות חדשות: `tests/repo/privacy-claims.test.ts`
  (grep ל‑`fetch(`/`analytics`/`cookie` ב‑`src/`, פטור שמי יחיד ל‑
  `legal-content.ts`) ו‑`tests/repo/licensing.test.ts` (`LICENSE`+`package.json`
  תואמים).
- `scripts/verify-privacy-a11y.mjs` נגד `npm run dev` אמיתי, Chromium 1194
  (`PW_CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` —
  playwright@1.63.0 בררת המחדל שלו ל‑headless_shell 1243 לא קיים בקונטיינר
  הזה): **17/17**. כולל ניגודיות נמדדת בפועל (לא מחושבת ידנית): טקסט גוף
  `rgb(134,141,155)` על `rgb(16,18,24)` = **5.61:1**; כותרת `rgb(238,240,245)`
  על אותו רקע = **16.42:1** — שני המספרים מעל סף ה‑WCAG AA (4.5:1).
- `change-reviewer` הופעל על הדיף המלא — ראו למטה.

## לא בתכולה (במפורש, לא בשתיקה)

- באנר הסכמת קוקיז, מסך הסכמת טפסים, זרימת החזרות/תשלום, הסרת ביקורות —
  שום אחד מהם לא קיים ב‑SoundGrid.
- ניווט חצים בין שורות ב‑Library — עקבי עם האתוס keyboard-first, אבל תוספת
  חדשה מעבר לתיקון הבאג.
- אכיפת CI אוטומטית שרשימת קרדיטי הקוד הפתוח מסונכרנת עם `package-lock.json`.
- שינוי גודל `LoadBtn`/איפור מצבי `AnalysisIcon` — חוב עיצוב אמיתי מהאודיט,
  לא בתכולת הגרסה הזו.

## תיקון המשך (14/09, אחרי המיזוג): התיקון החלקי יצר בעיה משלו

שלום דיווח, אחרי שה‑PR כבר מוזג, שלחיצת Tab "מסמנת את כל השירים ביחד". זה
לא היה נכון פשוטו כמשמעו (אף פעם לא יותר משיר אחד מודגש בבת אחת — אומת עם
30 שירים מזויפים ולחיצות Tab חוזרות), אבל הדיווח המדויק יותר שהתקבל אחרי
שאלת הבהרה חשף באג אמיתי: **המעבר בין שורה לתאים שלה (בורר ז'אנר, שדה
הערה, כפתורי Load) ביטל את טבעת הפוקוס לגמרי**, כי כל אחד מהם נשא בדיוק את
אותו צירוף `outline-none`+`focus-visible:outline-*` שתועד למעלה כחוב פתוח.
התוצאה בפועל: הטבעת "מהבהבת" רק בעצירה אחת מתוך חמש (השורה עצמה) ונעלמת
בארבע האחרות — בדיוק התחושה שהמשתמש תיאר, "המשתמש לא רואה איפה הפוקוס
שלו". ההחלטה המקורית "לתקן רק את השורה, זה חוב נפרד" הייתה שגויה בפועל:
תיקון חלקי של הפיצ'ר יצר חוויה גרועה יותר מלפני התיקון.

**תוקן במלואו** — אותה שורת קוד (`focus-visible:[--tw-outline-style:solid]`)
נוספה לשבעת המקומות שנשאו את הצירוף: שורת הטבלה (כבר היה), תיבת החיפוש,
בורר הז'אנר ושדה ההערה ב‑`Library.tsx`, ה‑`<select>` ב‑`Settings.tsx`/
`TopBar.tsx`, השדה ב‑`AiControlBar.tsx`, ו‑`HintIcon` ב‑`controls.tsx`.
`scripts/verify-privacy-a11y.mjs` קיבל שתי בדיקות חדשות שמדמות בדיוק את
התרחיש שדווח (Tab מהשורה לארבעת התאים שלה ולשורה הבאה, כל עצירה חייבת
`outlineStyle: solid`) — **19/19**. `npm run check` ירוק (אותם 8 כשלים
ידועים). חוב `--tw-outline-style` סגור לגמרי; אין יותר רכיב שנושא את
הצירוף השבור.
