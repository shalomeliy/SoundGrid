# SoundGrid — הוראות עבודה בפרויקט (עברית)

> **זו הגרסה העברית של [`CLAUDE.md`](CLAUDE.md).** הקובץ האנגלי הוא הקובץ הקובע —
> הוא זה שנטען אוטומטית בתחילת כל שיחה. הקובץ הזה קיים כדי שבעל הפרויקט, שאינו
> מתכנת, יוכל לקרוא ולהבין כל שורה.
>
> **כלל מחייב: כל שינוי באחד מהשניים חייב להיכנס גם לשני, באותו commit.**
> אסור שהם ייפרדו. אם מצאת שהם כבר נפרדו — ליישר אותם לפני שממשיכים בעבודה.

---

## מה המוצר

תוכנת DJ שרצה בדפדפן. שני דקים בסגנון Serato, מנגנת את המוזיקה שכבר נמצאת
על המחשב של המשתמש, ונשלטת מקונטרולר חומרה (Pioneer DDJ-FLX4).

הכל נבנה מאפס על יכולות הדפדפן הפתוחות — **Web Audio** (סאונד), **Web MIDI**
(קונטרולר), **File System Access** (קריאת קבצים מהדיסק).

> **אפס קוד, נכסים או דאטה מ‑Serato, מ‑rekordbox או מכל מוצר מסחרי אחר.**
> לקרוא תגיות מהקבצים של המשתמש — מותר. להעתיק קוד שלהם — לא.

היעד ארוך הטווח (`docs/architecture/directions.md`): **גרסאות דסקטופ ל‑Windows
ול‑Mac** דרך Tauri, לקראת v1.0. לכן הליבה (`core/`) לא מניחה שהיא רצה בדפדפן.

רץ על **Chromium בדסקטופ בלבד** (Chrome / Edge). זו החלטה מודעת: רק שם קיימות
Web MIDI, File System Access ובחירת כרטיס קול ספציפי.

**המסך של המשתמש:** בערך 1536×710 פיקסלים לדף. לתכנן פריסה לגובה ~710.
בנוסף, בגלל צפיפות המסך שלו **הכל נראה קטן ב‑24% פיזית** ממה שהמספר אומר —
להכפיל כל גודל CSS ב‑0.76 כדי לדעת מה הוא באמת רואה.

---

## מאיזה חלקים הפרויקט בנוי

React 19 + TypeScript + Vite, עיצוב ב‑Tailwind, ניהול מצב ב‑zustand.
אין שרת ואין בסיס נתונים — זו אפליקציה סטטית שרצה כולה בדפדפן, וזה במכוון:
בלי תלות בשרת אפשר להריץ אותה אופליין, ובהמשך גם כאפליקציית דסקטופ.

### שלוש השכבות

הקוד ב‑`src/` מחולק לשלוש שכבות. החלוקה **נאכפת אוטומטית** על ידי כלי בשם
dependency-cruiser — לא רק לפי הסכמה בין אנשים:

| שכבה | מה יושב שם | הכלל |
| --- | --- | --- |
| `src/core/` | הלוגיקה הטהורה — טיפוסים, קבועים, מיפוי MIDI, המלצות, והממשקים שב‑`ports/` | **אסור** לגעת ב‑React, ב‑DOM, בסאונד או ב‑MIDI |
| `src/platform/` | המימושים בפועל — `audio-webaudio/`, `source-fsaccess/`, `transport-webmidi/`, `analyzer-js/` | מממש את הממשקים שהוגדרו ב‑`core/ports/` |
| `src/app/` | כל ה‑React — `App.tsx`, `components/`, `hooks/`, `state/store.ts` | פה מותר הכל |

הרעיון: אם הליבה לא מכירה את הדפדפן, מעבר לדסקטופ הוא "כתיבת מתאם" ולא
"כתיבה מחדש".

### `src/controls.ts` — נקודת המעבר היחידה

**כל** פעולת משתמש עוברת דרך הקובץ הזה — עכבר, מקלדת, קונטרולר MIDI, ובעתיד
גם AI. הוא יושב מחוץ לשלוש השכבות בכוונה. זה מה שמבטיח שסיבוב כפתור בחומרה
וגרירת עכבר על המסך עושים בדיוק את אותו דבר.

### ייבוא קבצים

תמיד דרך הקיצור `@/...`, אף פעם לא `../`. `../types` לא מגלה איזו שכבה חצינו;
`@/core/types` כן. זה בדיוק מה שהסתיר את המבנה קודם (commit `f8f5cf8`).

---

## לפני שכותבים קוד

1. **לקרוא `HANDOFF.md` ראשון.** שם כתוב איפה הדברים עומדים: הגרסה הנוכחית,
   הצעד הבא, החלטות פתוחות וחובות ידועים. אחר כך סעיף הגרסה הרלוונטי
   ב‑`ROADMAP.md`, ואז `git log --oneline -5`.
2. **להרים את שרת הפיתוח לפני הכל.** `preview_start` עם `soundgrid-dev` מתוך
   `.claude/launch.json`. **לא** `npm run dev` דרך Bash — שרת שמורם ככה לא שורד
   ואי אפשר להגיע אליו מהכלים. המשתמש בודק את המערכת בכרום שלו במקביל לעבודה,
   אז שיחה שמתחילה בלי שרת גוזלת ממנו את כל תחילת השיחה.
3. **לנווט בקוד עם `codegraph_explore`** (יש `.codegraph/` בפרויקט) במקום
   לקרוא ולחפש בקבצים באופן גורף.
4. לפני שמוסיפים תלות חדשה בדפדפן — לבדוק את `docs/architecture/directions.md`.
   קרוב לוודאי שהממשק כבר מוגדר שם.
5. לכתוב מטרה, מה **לא** בתכולה, קריטריוני קבלה ותוכנית אימות — לפני נגיעה בקוד.
   להיעזר בסוכנים הקוראים־בלבד שב‑`.claude/agents/` (product, architecture,
   design, QA; ו‑security בכל שינוי שנוגע בקבצים של המשתמש). **כל החלטת מוצר
   מאושרת על ידי המשתמש.**
6. **להסביר את השינוי בעברית לפני שכותבים אותו.**

---

## גבולות — מה מותר ומה אסור

### הכלל המרכזי: לא מדלגים בשקט

המשתמש בחר את תיקיית Techno, ראה 42 טראקים — ובתיקייה היו 47 קבצים. הסריקה
פשוט עברה ליד מה שלא זיהתה, והפער היה בלתי נראה (commit `59c5fe5`).
זה הכלל הכי חשוב בפרויקט, והוא גובר על נוחות:

- כל דבר שנזרק, מסונן, מדולג, או שירדנו איתו ל‑fallback — **מוצג למשתמש
  במסך**, ובשם: אילו פורמטים, כמה.
- מה שמוסתר בכוונה מוסתר דרך **רשימה בעלת שם**, לא דרך השמטה. `COMPANION_EXT`
  ב‑`library.ts` מסתיר `.serato-stems` / `.asd` / `.reapeaks` / `.ovw`, ובדיוק
  בגלל זה תווית "N skipped" לא סופרת אותם.
- כל `catch` שבולע שגיאה, כל יציאה מוקדמת מפונקציה, כל ערך ברירת מחדל שמחליף
  בשקט — חייב לענות על השאלה: **איך המשתמש יֵדע?**
- זה לא נגמר בספרייה. יכולת חסרה בדפדפן מתדרדרת **בגלוי** (`Capabilities`,
  §3 ב‑`directions.md`): שום פיצ'ר לא קורס כשיכולת חסרה, ושום פיצ'ר לא מעמיד
  פנים שהוא עובד.

### שאר הכללים

- **לשמור על `core/` נקייה.** תלות בדפדפן → ממשק ב‑`core/ports/`, מימוש
  ב‑`platform/`. `npm run check` נכשל אחרת.
- **פעולת משתמש חדשה** → מוסיפים ל‑`controls.ts`, וקוראים לה גם מה‑UI וגם
  מ‑`transport-webmidi/manager.ts:dispatch`.
  **פעולת MIDI חדשה** → `ControlAction` ב‑`core/mapping/mapping.ts` + case
  ב‑`dispatch` + שורה ב‑`mappings/flx4.ts`.
- **מנוע האודיו אימפרטיבי וחי מחוץ ל‑React.** ה‑store מחזיק רק מידע פשוט
  שאפשר לשמור, לא אובייקטים חיים של סאונד.
- **קוראים את הקבצים של המשתמש, אף פעם לא כותבים אליהם.** `tags.ts` קורא רק
  קטעי בייטים קטנים, אף פעם לא טוען טראק שלם לזיכרון ואף פעם לא כותב חזרה.
- **אזהרה אחת פתוחה בכוונה:** `transport-webmidi/manager.ts` כותב ישירות
  ל‑store וקורא ל‑`controls.ts` במקום לעבור דרך הממשק. היא מוגדרת כ‑`warn`
  כדי שתישאר גלויה — לא להעתיק את הדפוס, ולא להשתיק את הכלל.
- **הערות בקוד מסבירות *למה* משהו קיים**, לא מה שהקוד כבר אומר. שתי דוגמאות
  לחקות: ההערה על `position:absolute` בקנבס של ה‑waveform, וההערה על קפיצה
  לפי offset בצ'אנקים של WAV ב‑`tags.ts`. כל אחת מהן מתעדת באג שיחזור מיד
  אם מישהו "יפשט" את הקוד.
- **לא לשבור** את ה‑fallback לסטריאו ולא את השליטה במקלדת.
- להעדיף פשוט ותחזיקתי על פני חכם. לא לשכתב אזורים לא קשורים.

---

## מתי משימה נחשבת גמורה

> **`vitest` נכנס ב‑v0.2.1** — `npm test`, ו‑`npm run check` מריץ אותו.
> מה שיושב ב‑`tests/repo/` הוא אינווריאנטים על **הריפו כמערכת**: גודל המסמכים,
> פער בין מספרי גרסה, נתיבים מתים, SHA שלא קיים, שני קבצי ה‑CLAUDE שזזים יחד,
> שאין קובץ שנושא סימני קונפליקט, ושאף קבוע כיול לא מגיע למסך ההגדרות.
> הכלל שמאחוריהם: **כל באג שנתפס פעם אחת הופך לבדיקה קבועה.** כשאתה מתקן משהו
> שבדיקה הייתה תופסת — תוסיף את הבדיקה באותו commit.

**למנוע האודיו ולספרייה עדיין אין בדיקות אוטומטיות**, ו‑runner לא ממציא אותן:
המנוע דורש `AudioContext`, הספרייה דורשת את הקבצים האמיתיים של המשתמש, וזיוף של
אחד מהם היה בודק את הזיוף. (‏`tests/core/` היא הדוגמה ההפוכה ומה שכדאי לחקות —
ב‑v0.2.2 חשבון הסקראצ' הוצא ל‑`core/scratch.ts` ויצאו ממנו 12 מקרים אמיתיים.) לכן "בדיקה שנופלת בלי השינוי" עדיין פירושה בדרך כלל
**אימות מפורש ומתועד**. הדוגמה לחקות היא v0.1.7: סקריפט Node קטן שרץ ישירות מול
`C:\Users\Shalom\Music\Tracks`, עם המספרים רשומים (BPM 96.9%, Key 97.2%,
Duration 100%, 0.6 שניות על 360 קבצים). ללוגיקה טהורה ב‑`core/` — mapping,
`recommend`, `parseKey` — אין את התירוץ הזה, והיא צריכה לקבל בדיקות אמיתיות
ככל שנוגעים בה.

שינוי גמור רק כאשר:

- **`npm run check` ירוק** — טיפוסים (tsc), סגנון (oxlint), אינווריאנטים (vitest) וכללי
  השכבות (depcruise). להריץ לפני כל commit.
- **קיימת בדיקה שהייתה נופלת בלי השינוי**, היא הורצה בפועל, והתוצאה רשומה —
  סקריפט, מדידה מול הספרייה האמיתית, או תצפית בדפדפן עם מספרים.
  **"זה בונה" זה לא אימות.**
- **נבדק באפליקציה הרצה**, לא רק במהדר. צילום מסך מה‑Browser pane לא עובד
  בסביבה הזו; מה שכן עובד: `javascript_tool` למדידת ה‑DOM ו‑
  `read_console_messages` לשגיאות. המשתמש מסתכל בכרום האמיתי שלו.
- **נמסר למשתמש, בכמה שורות פשוטות, איך הוא בודק את זה בעצמו.** ראה למטה.
- **`HANDOFF.md` מעודכן** — מצב, מה נעשה, מה הבא, ואילו חובות חדשים נוצרו.
  שיחה לא נסגרת בלי זה. **מה שלא כתוב שם — לא הועבר.**
- `ROADMAP.md` מסומן ✅ כשגרסה נסגרת.
- **ה‑diff עבר סקירה עצמאית** של `.claude/agents/change-reviewer.md`.
- הודעת ה‑commit מתחילה ב‑`vX.Y.Z:` ומספרת **מה היה שבור ואיך זה התגלה**,
  לא רק מה השתנה. `59c5fe5` ו‑`f8f5cf8` הם הרף.
- המשתמש יכול להסביר את ההחלטה, את מה שוויתרנו עליו, ומה הסיכון שנשאר.
- אם הקובץ הזה השתנה — `CLAUDE.md` השתנה איתו, ולהפך.

**אף פעם לא להחליש או למחוק בדיקה כדי שאימות יעבור.**

### למסור למשתמש איך לבדוק בעצמו

בסוף **כל גרסה, כל תת‑גרסה, וכל חלק עצמאי שסיימתי לקודד** — לכתוב למשתמש
כמה שורות קצרות בעברית: **איך לוודא בפועל, במערכת הרצה, שהדבר הזה עובד.**
צעדים קונקרטיים, לפי הסדר שבו יבצע אותם, ומה הוא אמור לראות:

- מה ללחוץ / מה לטעון (למשל: "לבחור את תיקיית Techno"),
- מה אמור להופיע (למשל: "בכותרת הספרייה תופיע התווית `1 skipped · mp4`"),
- והדבר האחד שאם יקרה — סימן שזה שבור.

כמה שורות, בלי ז'רגון. האימות שלי מוכיח שהקוד עובד; זה מה שמאפשר לבעל
הפרויקט לאשר את זה בעצמו — והוא לא מתכנת, אז "אומת" לא אומר לו כלום עד
שהוא יכול לשחזר את זה בעיניים שלו.

---

## ניהול ההקשר של השיחה

למה זה בכלל סעיף: כל סבב בשיחה קורא מחדש את כל מה שנכתב לפניו, אז העלות גדלה
עם **אורך השיחה**, לא עם קושי המשימה. לכן המצב חייב לחיות בקבצים, ושיחות
חייבות להיסגר.

- **`HANDOFF.md` נקרא ראשון בכל שיחה** והוא מקור האמת היחיד בין שיחות.
- **בסוף כל גרסה / תת‑גרסה להריץ:**
  `python C:/Users/Shalom/.claude/tools/context_check.py`
  אחרי שהבדיקות ירוקות, `HANDOFF.md` מעודכן והכל ב‑commit. **לפעול לפי
  התשובה, ולא לשאול את המשתמש אם להמשיך כאן או לפתוח שיחה חדשה** — המדידה
  הזו באה במקום השאלה הזו:
  - 🟢 — להתחיל את הגרסה הבאה כאן, בלי לשאול.
  - 🟡 — הגרסה סגורה ומאומתת, אז זה הרגע: לומר שהשיחה צריכה להיסגר.
  - 🔴 — לעצור. לומר שהשיחה חייבת להיסגר עכשיו.

  הספים יושבים בתוך הסקריפט — לכוונן שם, אף פעם לא לפי תחושה ברגע.
- **🟡/🔴 תמיד אומר שיחה חדשה, אף פעם לא `/clear`.** שניהם מוחקים את הזיכרון
  החי באותה מידה, אז לא הטוקנים הם שמכריעים. ההבדל הוא בתיעוד: `/clear` ממשיך
  לכתוב לאותו קובץ transcript, ולכן **לא** מאפס את המונים של
  `context_check.py` — אם ממשיכים לנקות, הכלי יראה 🔴 בזמן שההקשר בפועל ריק.
  בנוסף, transcript אחד לכל גרסה שומר על `--resume` ו‑`/rewind` שמישים.
  `/clear` מיועד רק לחקירה חד־פעמית שאין ערך לתעד.
  *(הסעיף הישן "קריטריון CLEAR" ב‑`HANDOFF.md` הוחלף בכלל הזה ונמחק
  בפיצול המסמכים של 29/08/2026.)*
- להעדיף סוכן־משנה לחקירה קוראת־בלבד ("איפה X בשימוש", "האם Y קיים") כדי
  שהקריאה שלו לא תיכנס להקשר של השיחה הזו.
- לא להדביק קבצים שלמים או פלט מלא של פקודה לשיחה כשטווח שורות ממוקד או
  ספירה עונים על השאלה.

---

## מפת המסמכים

כל מסמך עונה על שאלה אחת ולא נכנס לתחום של האחרים:

| השאלה | הקובץ |
| --- | --- |
| מה קורה **עכשיו** — גרסה, מה פתוח, הצעד הבא, חובות | `HANDOFF.md` |
| מה נבנה בגרסה **סגורה**, ומה נמדד שם | `docs/handoff/<version>.md` |
| מה נבנה **אחר כך**, באיזה סדר, ולמה הסדר השתנה | `ROADMAP.md` |
| **איפה יושב כל דבר** בעץ המקור | `docs/architecture/map.md` |
| **עקרונות ארוכי טווח** — הממשקים (ports), נקיון הליבה, חוזה שכבת ה‑AI, במה אנחנו שונים מ‑Serato/rekordbox, על איזו חומרה נרוץ | `docs/architecture/directions.md` |
| **הפורמטים של Serato** על הדיסק (database V2, crate TLV, GEOB: Markers2/BeatGrid/Autotags/Overview) | `docs/reference/serato-formats.md` |
| מה זו התוכנה, למישהו שנחת על הריפו | `README.md` |
| מה מותר ואיך עובדים | `CLAUDE.md` (אנגלית, הקובץ הקובע) + הקובץ הזה |

**הפיצול (2026-08-29).** `HANDOFF.md` הגיע ל‑43.9KB ו‑`ROADMAP.md` ל‑34.6KB — שניהם
append‑only, שניהם נקראים בתחילת כל שיחה, כלומר יחד עלות ההקשר הקבועה הגדולה ביותר
בריפו. אבל הבעיה החמורה יותר לא הייתה הגודל אלא שהקובץ **התחיל לשקר**: הכותרת שלו
הכריזה "v0.2.0b נכתב" בזמן ש**ארבעה מתוך שישה סעיפים בתכנון שלו עצמו מעולם לא בוצעו**,
קבורים בבלוק אחד מתוך עשרים. קובץ שגדל מפסיק להיקרא, וקובץ שלא נקרא מתחיל לשקר.

מעכשיו: `HANDOFF.md` מחזיק **את ההווה בלבד**, תחת **תקציב של 15,000 בתים** שכתוב
בראש הקובץ. כשגרסה נסגרת הבלוק שלה עובר ל‑`docs/handoff/<version>.md`, שמחזיק גם את
התכולה המקורית וגם את התוצאה. **כל מה שעדיין פתוח נשאר ב‑`HANDOFF.md`** — הארכיון
הוא לרשומה, לא למחבוא.

### אלה בדיקות עכשיו, לא מוסכמות (v0.2.1)

‏`tests/repo/` רץ בתוך `npm run check`, אז המסמכים נופלים בשער בדיוק כמו הקוד:

- **תקציב 15,000 הבתים נאכף.** כשהוא נופל — מוציאים בלוק סגור
  ל‑`docs/handoff/<version>.md`. **אף פעם לא מגדילים את התקציב.**
- **נתיב שמוזכר במסמך חייב להתקיים**, בשתי הצורות שהמסמכים כותבים: נתיב `src/…`
  מלא, או `dir/name` חשוף כמו במפת העץ. אם אתה מזכיר נתיב מת בכוונה כדי להסביר
  מעבר — סמן את השורה `<!-- dead-path -->`. פטור **בשם**, לא בהשמטה; אותו כלל
  כמו `COMPANION_EXT`.
- **הגרסה ב‑`package.json` ושורת "גרסה נוכחית" ב‑`HANDOFF.md` הן עובדה אחת.**
  לשנות את שתיהן יחד, ולתת לגרסה שורה ב‑`ROADMAP.md`.
- **SHA שמצוטט במסמך חייב להתקיים.** המסמכים כאן מנמקים דרך ההיסטוריה; SHA
  שלא קיים נקרא כמו ראיה ואיננו.
- **שני קבצי ה‑CLAUDE חייבים לזוז באותו commit** — לכלל שבראש הקובץ הזה יש
  סוף‑סוף משהו מאחוריו.
- **אף קובץ במעקב לא מכיל סימני קונפליקט** (v0.2.4). ‏`CLAUDE.md` ישב על `main` עם
  שני אזורי מיזוג לא‑פתורים בתוכו במשך שלוש גרסאות, ו‑`npm run check` נשאר ירוק כל
  הזמן הזה: חמש הבדיקות שמעל קוראות את המסמכים **למשמעות**, ואף אחת לא קראה אותם
  **לנזק**.
- **אף קבוע כיול לא מגיע למסך ההגדרות** (v0.2.5). ‏`PLATTER_SIZE`,
  ‏`POSITION_EPSILON_SEC`, ‏`DECLICK_SEC` והשאר — כל אחד מהם מקבע באג שכבר תוקן,
  ופקד למשתמש על מספר כזה הוא דרך מנומסת לתת לו לשבור לעצמו את התוכנה. זו הייתה
  פסקה ב‑`ROADMAP.md`, כלומר לא הייתה כלום. הבדיקה מסירה הערות קודם, כדי
  ש‑`core/settings.ts` יוכל עדיין **לנקוב בשמם** בפסקה שמסבירה למה הם בחוץ —
  פטור בשם, לא השמטה.

**נשאר מוסכמה, כי אין בדיקה שיכולה לבטא את זה: "שורת הסטטוס אומרת אמת".** הכשל של
29/08 — הכותרת שהכריזה "v0.2.0b נכתב" בזמן שארבעה מתוך שישה סעיפים בתכנון שלה
מעולם לא בוצעו — דורש בן אדם שיקרא את התכנון מול הקוד. הפיצול הוא מה שמאפשר את
הקריאה הזאת; הוא לא תחליף לה.

‏`ROADMAP.md` הוא ~32KB ו‑append‑only, והושאר בכוונה — מפרטי הגרסאות שבו נקראים
**בררנית**, סעיף אחד בכל פעם, אז הוא לא נושא את אותה עלות לכל שיחה. אין לו עדיין
בדיקת תקציב.

---

## פקודות שימושיות

```bash
npm run check
```

זו הפקודה החשובה — היא מריצה ארבע בדיקות ברצף: `tsc` (טיפוסים),
`oxlint` (סגנון קוד), `depcruise` (כללי השכבות) ו‑`vitest` (האינווריאנטים
של הריפו). **השער לפני כל commit.**

```bash
npm test
```

‏`vitest` לבד — האינווריאנטים שב‑`tests/repo/` והבדיקות שב‑`tests/core/`.

```bash
npm run build
```

בדיקת טיפוסים + בנייה סטטית לתיקיית `dist/`.

```bash
python C:/Users/Shalom/.claude/tools/context_check.py
```

בסוף כל גרסה — אומר 🟢 להמשיך כאן / 🟡🔴 לסגור את השיחה.

שרת פיתוח: `preview_start` עם `soundgrid-dev` (פורט 5173) — **לא** דרך Bash.

**אין בפרויקט הזה formatter ואין CI.** לא להניח שמשהו בודק את הענף
אוטומטית. `npm run check` הוא כל השער — והוא רץ רק כשמריצים אותו.
